//*Loops*
//Create a function that takes in a number. Console log the result of heads or tails using the previous function x times where x is the number passed into the function. Call the function.
    
    function loop(num){
        for(i = 0; i < ){

        }
    }

// shorten the function with turnery operators and arrow function
    function headOrTails(){
            let result = Math.random()
            if(result < .5){
                return 'heads'
            } else {
                return 'tails'
            }
        }

        const headOrTails = () => Math.random() < 0.5 ? 'heads' : 'tails'  

    


    // Write an arrow function expression called greet(). It should accept a single argument representing a person's name. It should return a greeting string
    greet("Hagrid"); //"Hey Hagrid!"
    greet("Luna"); //"Hey Luna!"
    