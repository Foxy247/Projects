//Create a function that takes in an array of numbers. Multiply each number together and alert the product. 
    // initialize product
    // 

function multi(array){
    let product = 1
    array.forEach
    (num => product = product * num)
    // 2 => 2 = 2 * 1
    // 4 => 8 = 2 * 4 
    // 6 => 48 = 8 * 6
    
    alert(product)
}
multi([2,4,6])

push 
pop 
shift
unshift

function abc(num){
    
    console.log(Math.cbrt(num).toFixed(2));
}
abc(9)

  